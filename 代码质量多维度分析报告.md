# ALE DAN CPL 系统 — 代码质量多维度分析报告

> 分析日期：2026-06-03
> 分析维度：可靠性 · 安全性 · 架构规范性 · 代码整洁性

---

## 总览

| 维度 | CRITICAL | HIGH | MEDIUM | LOW | 合计 |
|------|----------|------|--------|-----|------|
| 可靠性 | 1 | 4 | 8 | 4 | 17 |
| 安全性 | 2 | 5 | 6 | 4 | 17 |
| 架构规范性 | 1 | 7 | 10 | 5 | 23 |
| 代码整洁性 | 0 | 9 | 18 | 6 | 33 |
| **合计** | **4** | **25** | **42** | **19** | **90** |

---

## 一、CRITICAL 级别（必须修复）

### C1. `updateQuotation` 无事务保护 — 并发写入导致数据丢失
**维度：可靠性** · `server/db/quotations.ts:258-363`

`updateQuotation` 执行 5-6 个独立 SQL 操作（读旧项→删旧项→插新项→更新版本→插版本快照），无事务包裹。两个并发请求可交叉执行，导致报价单项丢失。若插入失败，报价单变为 0 项且无报错。

### C2. Admin 可创建 isSuperAdmin 用户 — 权限提升
**维度：安全性** · `server/routers/users.ts:28-56`

`users.create` 使用 `adminProcedure`（非 `superAdminProcedure`），且接受 `isSuperAdmin` 输入参数。普通管理员可通过创建新用户获取超级管理员权限。

### C3. JWT Secret 默认空字符串
**维度：安全性** · `server/_core/env.ts:3`

`process.env.JWT_SECRET ?? ""` — 若未设置环境变量，JWT 使用空密钥签名，token 可被轻易伪造。无启动时校验。

### C4. `as any` 类型逃逸违反项目规则
**维度：架构规范性** · 多处

CLAUDE.md 明确禁止 `any`，但生产代码中存在：
- `server/routers/templates.ts:42,66`
- `server/routers/sharing.ts:20`
- `server/db/quotations.ts:420`

---

## 二、HIGH 级别（应当尽快修复）

### 可靠性

| # | 问题 | 位置 |
|---|------|------|
| H1 | `deleteQuotation` 无事务 — 3 个 DELETE 可部分失败 | `server/db/quotations.ts:366-393` |
| H2 | `switchActiveImport` 非原子 — 中间状态导致 CPL 数据不可见 | `server/routers/importLogs.ts:50-63` |
| H3 | 报价单创建两阶段写入 — 第二阶段失败留下空报价单 | `server/routers/quotations.ts:65-103` |
| H4 | DB 不可用时静默返回成功 — 删除/更新操作无感知失败 | `server/db/index.ts` + 所有 `server/db/*.ts` |

### 安全性

| # | 问题 | 位置 |
|---|------|------|
| H5 | `getById` 无所有权校验 — 任意用户可查看他人报价单 | `server/routers/quotations.ts:32-43` |
| H6 | 版本历史无权限校验 — 任意用户可查看他人版本差异 | `server/routers/versions.ts:33-56` |
| H7 | eFlash 附件路径穿越 — `fileName` 未过滤可写入任意目录 | `server/routers/eflash.ts:291-322` |
| H8 | eFlash 静态文件无鉴权 — 任何人可下载上传的附件 | `server/_core/index.ts:91-94` |
| H9 | Storage Proxy 无鉴权 — 匿名用户可获取预签名 URL | `server/_core/storageProxy.ts:4-48` |

### 架构规范性

| # | 问题 | 位置 |
|---|------|------|
| H10 | 折扣计算逻辑在 router 中重复 | `server/routers/quotations.ts:78-93,139-155` |
| H11 | 授权逻辑手动内联，未使用权限系统 | `server/routers/quotations.ts` (4 处) |
| H12 | Excel 解析逻辑在 router 中（~300 行） | `server/routers/cpl.ts,eflash.ts,certifications.ts,productSpecs.ts` |
| H13 | 三种授权模式混用，无统一规范 | `adminProcedure` / `superAdminProcedure` / `permissionProcedure` + 手动检查 |
| H14 | `Record<string, any>` 列映射丢失类型安全 | `server/db/cpl.ts,quotations.ts,suggestions.ts` |
| H15 | 模块级 `importInProgress` 锁不支持多进程 | `server/routers/cpl.ts:10` |
| H16 | DB 层不应导入 TRPCError | `server/db/index.ts:20-24` |

### 代码整洁性

| # | 问题 | 位置 |
|---|------|------|
| H17 | 4 处 `Record<string, any>` 列映射 | `server/db/cpl.ts:142,185` · `quotations.ts:119` · `suggestions.ts:10` |
| H18 | SDK 中 6 处 `as any` 转型 | `server/_core/sdk.ts:138-142,249-253` |
| H19 | `isAdmin` 检查重复 6 次 | `server/routers/quotations.ts` |
| H20 | `csvEscape` 函数重复两处 | `server/routers/activityLogs.ts,importLogs.ts` |
| H21 | CPL 列定义和 localStorage 逻辑重复 ~200 行 | `DataViewer.tsx` + `ProductDataContent.tsx` |

---

## 三、MEDIUM 级别（计划修复）

### 可靠性
- M1. 限流器内存泄漏 — `loginAttempts` Map 无清理机制
- M2. 无优雅关闭 — 无 SIGTERM/SIGINT 处理，连接池不释放
- M3. `getBestMatchSet` 加载全部产品规格到内存
- M4. 证书导入逐行处理无事务 — 1000 行产生 4000 次 DB 往返
- M5. `clearImportLogs` 不清理关联数据 — 产生孤儿记录
- M6. `importCplOverwrite` 大事务无死锁重试
- M7. 删除 eFlash 先删 DB 后删文件 — 文件清理失败时无法追溯
- M8. `updateCertification` 删除-插入模式无事务

### 安全性
- M9. 保存搜索删除无所有权校验
- M10. 产品规格更新/删除无权限校验
- M11. 限流器仅对已认证 mutation 生效
- M12. `X-Forwarded-For` 可伪造绕过登录限流
- M13. `TRPCError.cause` 可能泄露内部错误详情到客户端
- M14. 共享报价单永久有效，返回完整客户信息

### 架构规范性
- M15. 错误消息中英文混杂（无统一规范）
- M16. eflash.ts 直接导入 `../db/eflash` 而非 barrel import
- M17. `ctx.user!` 非空断言冗余（`protectedProcedure` 已保证）
- M18. router 直接导入 Drizzle schema 类型
- M19. 版本 diff 计算逻辑在 router 中（~50 行）
- M20. `logActivity` 火-忘模式，错误不可观测
- M21. 缺少 feature 级 `api.ts` 文件
- M22. 输入验证不完整（空 productModel、负数价格）
- M23. 删除操作返回值不一致（void vs `{ success: true }`）
- M24. 限流用 `FORBIDDEN` 而非 `TOO_MANY_REQUESTS`

### 代码整洁性
- M25. 9 个文件超过 300 行（最大 1060 行）
- M26. `pageSize: 10000` 魔术数字出现 4 次
- M27. `console.log` 用于生产日志（应使用 pino/winston）
- M28. SDK 中 9 处 console 调用，前缀不统一
- M29. 客户端 localStorage 错误用 `console.error`（应静默或 toast）

---

## 四、LOW 级别（择机修复）

- L1. `as any` 类型转型在测试中（14 处）
- L2. Cookie `SameSite=none` 增大攻击面
- L3. 50MB body limit 全局生效
- L4. bcrypt 72 字节截断无提示
- L5. 死代码 — `shared/_core/errors.ts` HTTP 错误类未使用
- L6. 注释掉的 14 行域名逻辑
- L7. `parseExcelDate` 无意义别名
- L8. 缺少 JSDoc（`getQuotationAnalytics` 146 行无文档）
- L9. `ChevronRight` 重复导入
- L10. 缺少 `app/` 目录（router/layout/providers）

---

## 五、优先修复建议

### 第一优先级：数据完整性 + 权限提升（CRITICAL）

1. **为 `updateQuotation`/`deleteQuotation`/`batchDeleteQuotations` 添加事务**
2. **`users.create` 改用 `superAdminProcedure` 或移除 `isSuperAdmin` 输入**
3. **JWT Secret 启动校验 — 缺失时拒绝启动**
4. **消除所有 `as any` — 补全缺失的类型定义**

### 第二优先级：数据泄露 + 路径穿越（HIGH 安全）

5. **`getById`/`versions` 添加所有权校验**
6. **eFlash `fileName` 路径过滤 + 文件类型白名单**
7. **eFlash 静态文件和 Storage Proxy 添加鉴权**

### 第三优先级：架构治理（HIGH 架构）

8. **提取折扣计算到共享工具函数**
9. **提取授权检查到中间件（`isManagerOrAdmin` helper）**
10. **提取 Excel 解析到 `server/lib/` 模块**
11. **统一授权模式 — 全部使用 `permissionProcedure`**

### 第四优先级：代码质量（MEDIUM）

12. **提取 `csvEscape`、`isAdmin`、列映射到共享模块**
13. **统一错误消息语言（英文 + 前端 i18n）**
14. **DB 层去掉 TRPCError 依赖**
15. **添加优雅关闭和进程级错误处理**
